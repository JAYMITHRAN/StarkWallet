# StarkMoneyWalletTracker keeps environment variables per-workspace, since
# the client (Vite) and server (Node) load env differently. Copy each of
# these before running the app:
#
#   cp server/.env.example server/.env
#   cp client/.env.example client/.env
#
# See docs/ENVIRONMENT.md for what every variable does.
