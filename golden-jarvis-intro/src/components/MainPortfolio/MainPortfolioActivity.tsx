import "./MainPortfolioActivity.css";

/**
 * Stand-in destination screen. Replace with your real portfolio
 * routes/components — this only exists so the intro sequence has
 * somewhere concrete to hand off to.
 */
export default function MainPortfolioActivity() {
  return (
    <div className="fade-layer fade-in intro-stage main-portfolio">
      <h1>Welcome to the Portfolio</h1>
      <p>Arc Reactor fully charged — systems online.</p>
    </div>
  );
}
