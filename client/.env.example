# The client talks to the API through Vite's dev proxy (see vite.config.ts),
# so no VITE_API_URL is needed in development. Add one here if you deploy
# the client and server on different origins in production.
# VITE_API_URL=https://api.yourdomain.com
